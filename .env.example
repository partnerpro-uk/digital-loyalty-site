# Sanity CMS Configuration
# These are optional - defaults are hardcoded for this project
SANITY_STUDIO_PROJECT_ID=zutnuftx
SANITY_STUDIO_DATASET=production

# Alternative env var names (also supported)
# PUBLIC_SANITY_PROJECT_ID=zutnuftx
# PUBLIC_SANITY_DATASET=production

# DeepL API (for translations - optional)
# DEEPL_API_KEY=your-api-key

# Netlify Functions (for lead capture - optional)
# SANITY_API_TOKEN=your-write-token
